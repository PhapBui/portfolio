import { Paths } from 'models';

export const pathConfig: Paths = {
  root: '/',
  counter: '/counter',
  products: '/products',
  about: '/about',
  checkout: '/checkout',
  cart: '/cart',
  notFound: '*',
};
